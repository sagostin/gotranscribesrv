# GoTranscribeSrv — Python Inference Sidecar
# Requirements

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# ML / ASR — MLX-native Parakeet for Apple Silicon
parakeet-mlx>=0.4.0

# ML / Diarization — Sortformer (NeMo)
torch>=2.4.0
nemo_toolkit[asr]>=2.1.0

# TTS — LuxTTS (clone https://github.com/ysharma3501/LuxTTS into sidecar/)
# Not a pip package — resolved via PYTHONPATH or sys.path

# VAD
silero-vad>=5.1

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0
numpy>=1.26.0

# WebSocket
websockets>=13.0

# Utilities
pydantic>=2.9.0
pydantic-settings>=2.6.0
