<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  GoTranscribeSrv — sidecar log rotation companion.

  Runs rotate-sidecar-logs.sh hourly (and once at load) so the launchd
  flat-file logs in deploy/macos/logs/ can never fill the disk. Rotates:
    - swift-sidecar.{out,err}.log  (legacy audio-sidecar shim — transition)
    - audio-sidecar.{out,err}.log   (current audio sidecar)
    - llm-sidecar.{out,err}.log     (LLM sidecar)
  Installed by `make sidecar-install` (audio) and `make llm-install`.
-->
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>com.gotranscribesrv.swift-sidecar-logrotate</string>

	<key>ProgramArguments</key>
	<array>
		<string>__REPO_PATH__/deploy/macos/rotate-sidecar-logs.sh</string>
	</array>

	<key>WorkingDirectory</key>
	<string>__REPO_PATH__</string>

	<key>RunAtLoad</key>
	<true/>

	<!-- Hourly: bounds worst-case overshoot past 10 MB to one hour of logs -->
	<key>StartInterval</key>
	<integer>3600</integer>
</dict>
</plist>
