# ── Binaries ────────────────────────────────────
bin/
*.exe

# ── Environment ─────────────────────────────────
.env

# ── Go ──────────────────────────────────────────
*.test
coverage.out

# ── Python ──────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# ── ML Models (cached via HuggingFace) ─────────
*.pt
*.onnx
*.nemo
sample_test.mp3

# ── IDE / OS ────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Deploy (launchd sidecar logs, local to each node) ──
deploy/macos/logs/

# ── DB backups (local dumps from make db-backup) ──
backups/

# ── Real Caddyfile with node IPs (copy from Caddyfile.example) ──
/Caddyfile
/sidecar-audio/.build/
/sidecar-audio/Vendor/text-processing-rs/
/sidecar-audio/Vendor/text-processing-rs/target/

# ── LLM sidecar vendored dep (patched swift-embeddings — `make llm-vendor`) ──
/sidecar-llm/vendor/swift-embeddings/
/docs/cost-benefit-analysis.md
/docs/email-cost-benefit-draft.md
